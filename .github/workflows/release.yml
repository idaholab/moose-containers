name: Release

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run without making changes"
        required: false
        type: boolean
        default: true

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  prepare:
    name: prepare
    runs-on: ubuntu-slim
    permissions:
      contents: read
      packages: read
    outputs:
      from-base-rocky8: ${{ steps.prepare.outputs.from-base-rocky8 }}
      from-mpi-rocky8-cuda-gcc: ${{ steps.prepare.outputs.from-mpi-rocky8-cuda-gcc }}
      from-mpi-rocky8-gcc: ${{ steps.prepare.outputs.from-mpi-rocky8-gcc }}
      from-mpi-rocky8-oneapi: ${{ steps.prepare.outputs.from-mpi-rocky8-oneapi }}
      from-mpi-rocky9-gcc: ${{ steps.prepare.outputs.from-mpi-rocky9-gcc }}
      from-mpi-ubuntu24-cuda-gcc: ${{ steps.prepare.outputs.from-mpi-ubuntu24-cuda-gcc }}
      from-mpi-ubuntu24-clang: ${{ steps.prepare.outputs.from-mpi-ubuntu24-clang }}
      from-mpi-ubuntu24-gcc: ${{ steps.prepare.outputs.from-mpi-ubuntu24-gcc }}
      from-mpi-ubuntu24-gccmin: ${{ steps.prepare.outputs.from-mpi-ubuntu24-gccmin }}
      to-base-rocky8: ${{ steps.prepare.outputs.to-base-rocky8 }}
      to-mpi-rocky8-cuda-gcc: ${{ steps.prepare.outputs.to-mpi-rocky8-cuda-gcc }}
      to-mpi-rocky8-gcc: ${{ steps.prepare.outputs.to-mpi-rocky8-gcc }}
      to-mpi-rocky8-oneapi: ${{ steps.prepare.outputs.to-mpi-rocky8-oneapi }}
      to-mpi-rocky9-gcc: ${{ steps.prepare.outputs.to-mpi-rocky9-gcc }}
      to-mpi-ubuntu24-cuda-gcc: ${{ steps.prepare.outputs.to-mpi-ubuntu24-cuda-gcc }}
      to-mpi-ubuntu24-clang: ${{ steps.prepare.outputs.to-mpi-ubuntu24-clang }}
      to-mpi-ubuntu24-gcc: ${{ steps.prepare.outputs.to-mpi-ubuntu24-gcc }}
      to-mpi-ubuntu24-gccmin: ${{ steps.prepare.outputs.to-mpi-ubuntu24-gccmin }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install python
        shell: bash
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          uv sync --frozen
      - name: Prepare
        id: prepare
        shell: bash
        run: |
          source .venv/bin/activate
          python ./.github/scripts/ci.py prepare_release --github-token "${{ secrets.GITHUB_TOKEN }}"

  merge-release:
    name: merge-release
    needs: prepare
    if: ${{ needs.prepare.result == 'success' && !inputs.dry_run }}
    runs-on: ubuntu-slim
    environment: ${{ !inputs.dry_run && 'release' || '' }}
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Check branch
        shell: bash
        run: |
          if [ "${{ github.ref }}" != "refs/heads/main" ]; then
            echo "Error: This workflow can only be run on 'main'"
            exit 1
          fi
      - name: Merge main into release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout release
          git merge --no-ff origin/main -m "Merge main into release"
          git push origin release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  base-rocky8:
    name: "base-rocky8"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-base-rocky8
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-base-rocky8 }}
          to-uri: ${{ needs.prepare.outputs.to-base-rocky8 }}

  mpi-rocky8-cuda-gcc:
    name: "mpi-rocky8-cuda-gcc"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-rocky8-cuda-gcc
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-rocky8-cuda-gcc }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-rocky8-cuda-gcc }}

  mpi-rocky8-gcc:
    name: "mpi-rocky8-gcc"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-rocky8-gcc
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-rocky8-gcc }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-rocky8-gcc }}

  mpi-rocky8-oneapi:
    name: "mpi-rocky8-oneapi"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-rocky8-oneapi
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-rocky8-oneapi }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-rocky8-oneapi }}

  mpi-rocky9-gcc:
    name: "mpi-rocky9-gcc"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-rocky9-gcc
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-rocky9-gcc }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-rocky9-gcc }}

  mpi-ubuntu24-cuda-gcc:
    name: "mpi-ubuntu24-cuda-gcc"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-ubuntu24-cuda-gcc
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-ubuntu24-cuda-gcc }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-ubuntu24-cuda-gcc }}

  mpi-ubuntu24-clang:
    name: "mpi-ubuntu24-clang"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-ubuntu24-clang
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-ubuntu24-clang }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-ubuntu24-clang }}

  mpi-ubuntu24-gcc:
    name: "mpi-ubuntu24-gcc"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-ubuntu24-gcc
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-ubuntu24-gcc }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-ubuntu24-gcc }}

  mpi-ubuntu24-gccmin:
    name: "mpi-ubuntu24-gccmin"
    needs: [prepare, merge-release]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      needs.merge-release.result == 'success'
      && needs.prepare.outputs.to-mpi-ubuntu24-gccmin
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Release
        uses: ./.github/actions/release
        with:
          registry-token: ${{ secrets.GITHUB_TOKEN }}
          from-uri: ${{ needs.prepare.outputs.from-mpi-ubuntu24-gccmin }}
          to-uri: ${{ needs.prepare.outputs.to-mpi-ubuntu24-gccmin }}

  finalize:
    name: "finalize"
    needs:
      - base-rocky8
      - mpi-rocky8-cuda-gcc
      - mpi-rocky8-gcc
      - mpi-rocky8-oneapi
      - mpi-rocky9-gcc
      - mpi-ubuntu24-cuda-gcc
      - mpi-ubuntu24-clang
      - mpi-ubuntu24-gcc
      - mpi-ubuntu24-gccmin
    if: >-
      !cancelled()
      && contains(fromJSON('["success", "skipped"]'), needs.base-rocky8.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-rocky8-cuda-gcc.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-rocky8-gcc.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-rocky8-oneapi.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-rocky9-gcc.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-ubuntu24-clang.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-ubuntu24-cuda-gcc.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-ubuntu24-gcc.result)
      && contains(fromJSON('["success", "skipped"]'), needs.mpi-ubuntu24-gccmin.result)
    runs-on: ubuntu-slim
    permissions:
      contents: read
      packages: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Install python
        shell: bash
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          uv sync --frozen
      - name: Check
        shell: bash
        run: |
          source .venv/bin/activate
          python ./.github/scripts/ci.py" \
            post_release \
            --github-token "${{ secrets.GITHUB_TOKEN }}"
