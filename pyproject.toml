[project]
name = "moose-containers"
version = "0.1.0"
description = "Utilities for building MOOSE base containers"
requires-python = ">=3.14"
dependencies = [
    "jinja2",
    "pyyaml",
    "tabulate",
    "requests",
    "ruff",
]
